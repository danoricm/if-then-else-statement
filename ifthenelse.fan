// ifthenelse.fan
class IfThenElse {
    static Void main() {
        Int x := 5
        if (x > 6) {
            echo("x is greater than 6")
        } else {
            echo("x is less than or equal to 6")
        }
    }
}
